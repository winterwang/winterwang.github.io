library(blogdown)

blogdown::new_post(
  title  = "2025年10月22日 星期三", # change to today
  ext    = ".md",
  subdir = "post",
  slug   = "2025-10-22",   # 建議加英文 slug，避免網址含中文
  categories = c("Life"),
  tags       = c("日常","記錄","work")
)